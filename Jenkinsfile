pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                //echo 'Running build automation' > test.txt
                  }
        }
        stage('test') {
	}
    }
}

